build:
  builder: DOCKERFILE
  dockerfilePath: Dockerfile

deploy:
  startCommand: /bin/blackbox_exporter --config.file=/etc/blackbox_exporter/config.yml --web.listen-address=:${PORT:-9115}
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10
